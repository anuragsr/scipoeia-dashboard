import { CheckedResultsPipe } from './checked-results.pipe';

describe('CheckedResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckedResultsPipe();
    expect(pipe).toBeTruthy();
  });
});
